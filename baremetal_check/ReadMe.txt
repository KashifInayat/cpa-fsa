For running the baremetal test frist you need to generate the baremetal binariese files and then need to use that files through the below commands to test the module functionality:


1) ./make.sh
2) ./simv ./matmul_os-baremetal
